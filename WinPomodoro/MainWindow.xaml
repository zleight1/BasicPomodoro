<Window x:Class="WinPomodoro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BasicPomodoro" Height="350" Width="525">
    <StackPanel>
        <Grid>
            <TextBlock>Timer</TextBlock>
        </Grid>
        <Grid>
            <TextBlock>Task</TextBlock>
        </Grid>
        <Grid>
            <TextBlock>Info</TextBlock>
        </Grid>
        <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Content="Start" ToolTip="Start the timer" Margin="5" Grid.Column="0"></Button>
            <Button Content="Stop" ToolTip="Stop the timer, without resetting" Margin="5" Grid.Column="1"></Button>
            <Button Content="Reset" ToolTip="Stop, and reset the timer and tasks" Margin="5" Height="22" Grid.Column="2"></Button>
        </Grid>
    </StackPanel>
</Window>
